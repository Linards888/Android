// The one global `RobotState state;` is defined directly in Android.ino
// (declared `extern` in RobotState.h), not here. This file is intentionally
// empty - kept so the sketch doesn't need reconfiguring.
