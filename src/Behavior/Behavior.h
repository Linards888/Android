#pragma once

// No longer used - the state machine and driving algorithm moved back into
// Android.ino directly (setup(), loop(), and one function per state,
// including onRunning() where you write your own algorithm). See
// docs/Android_ino.md. This file is kept as an empty placeholder.
